Ballot Image
==========

This software is for extracting ballot imformation from hand marked ballots that have been generated by the software.

Building
--------
BallotImage uses Maven for building.

You can build the project using `mvn clean package`.  This will install the project into your local Maven repository.

